# Copy to .env for local use, or set in Dokploy's Environment tab.
# Use the public IPv4 address or a DNS-only hostname pointing to your server.
FTP_PUBLIC_HOST=203.0.113.10
# Replace with a unique password of at least 16 characters.
FTP_PASSWORD=replace-with-a-long-random-password
FTP_PORT=21
TZ=Asia/Kolkata
CATALINA_OPTS=-Xms256m -Xmx1024m -Djava.awt.headless=true
